<!DOCTYPE html>
<html>
<head>
  <title>Restaurants Near You</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      padding: 2rem;
      background: #f3f4f6;
    }

    .top-right {
      position: absolute;
      top: 1rem;
      right: 1rem;
    }

    .btn {
      padding: 0.5rem 1rem;
      margin: 0.5rem;
      border: none;
      background-color: #3b82f6;
      color: white;
      border-radius: 5px;
      cursor: pointer;
    }

    .btn:hover {
      background-color: #2563eb;
    }

    h1 {
      text-align: center;
      margin-bottom: 2rem;
    }

    ul {
      list-style-type: none;
      padding: 0;
      max-width: 800px;
      margin: auto;
    }

    li {
      background: white;
      padding: 1rem;
      margin-bottom: 1rem;
      border-radius: 6px;
      box-shadow: 0 0 4px rgba(0,0,0,0.1);
    }
  </style>
</head>
<body>

<div class="top-right">
  <form action="/dashboard" method="GET">
    <input type="hidden" name="userId" value="<%= userId %>">
    <button class="btn" type="submit">Dashboard</button>
  </form>
</div>

<h1>Restaurants Near You</h1>

<% if (error) { %>
  <p style="color: red; text-align: center;"><%= error %></p>
<% } else if (restaurants.length === 0) { %>
  <p style="text-align: center;">No restaurants found in your area.</p>
<% } else { %>
  <ul>
    <% restaurants.forEach(restaurant => { %>
      <li>
        <strong>Name:</strong> <%= restaurant.name %> <br>
        <strong>Location:</strong> <%= restaurant.location %> <br>
        <strong>Phone:</strong> <%= restaurant.phone %> <br>
        <strong>Rating:</strong> <%= restaurant.rate %> ⭐ <br>
        <strong>Type:</strong> <%= restaurant.rest_type %> <br>
        <strong>Online Order:</strong> <%= restaurant.online_order ? 'Yes' : 'No' %> <br>
        <strong>Book Table:</strong> <%= restaurant.book_table ? 'Yes' : 'No' %> <br>
        <% if (restaurant.url) { %>
          <a href="<%= restaurant.url %>" target="_blank">Visit Website</a>
        <% } %>
      </li>
    <% }) %>
  </ul>
<% } %>

</body>
</html>
